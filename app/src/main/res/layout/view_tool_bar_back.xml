<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
                                   xmlns:tools="http://schemas.android.com/tools"
                                   android:layout_width="match_parent"
                                   android:layout_height="@dimen/tool_bar_height"
                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                   android:background="?attr/colorPrimaryDark"
                                   app:navigationIcon="@drawable/icon_arrow_left_neg_m"
    >

  <LinearLayout
      android:layout_width="288dp"
      android:layout_height="40dp"
      android:orientation="vertical"
      android:visibility="gone"
      >

    <TextView
        android:id="@+id/toolbarBackTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Sant Feliu de Codines"
        style="@style/toolbar_title"
        />

    <TextView
        android:id="@+id/toolbarBackSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="121.647 inmuebles"
        style="@style/toolbar_subtitle"
        />

  </LinearLayout>

</android.support.v7.widget.Toolbar>